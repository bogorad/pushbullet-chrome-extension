# Bare multiline code layout

- Allowed verification-code phrases followed directly by a newline and token, such as `Your verification code\n123456`.
- Kept the newline-separated non-code token guard in place so help text followed by ticket IDs still does not create a copy-code action.
