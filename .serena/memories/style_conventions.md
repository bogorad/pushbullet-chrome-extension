Use 2-space indentation, single quotes, semicolons, ES2022 target. Prefer strict TypeScript, explicit types over any, interfaces for complex objects, async/await. Imports grouped stdlib -> third-party -> local. Naming: camelCase vars/functions, PascalCase types, SCREAMING_SNAKE_CASE constants, prefix unused params with underscore or existing convention. Follow repository/event-bus/state-machine/single-flight patterns where already present. Keep logs centralized and avoid sensitive data in logs.