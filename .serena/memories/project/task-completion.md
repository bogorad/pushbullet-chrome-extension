When code changes are made, run relevant quality gates: npm run test, npm run typecheck, npm run lint, and npm run build unless task scope makes a smaller targeted run more appropriate during iteration. This repo uses beads (bd) for task tracking; update bead status as work progresses. Do not commit or push unless the user explicitly asks. Verify any popup/background/session contract changes with tests because service worker lifecycle bugs are common in this codebase.