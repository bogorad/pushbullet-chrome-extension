Development commands: npm run build (build all extension components), npm run typecheck, npm run lint, npm run lint:fix, npm run test, npm run test:watch, npm run test:coverage, vitest run path/to/test.ts (single test file), node ./scripts/bump-patch.cjs (bump patch). Useful repo commands: git status, git diff, git log --oneline, bd ready --json, bd update <id> --claim --json, bd close <id> --reason "Completed" --json.